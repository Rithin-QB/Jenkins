@Library('my-shared-library') _

def env = params.env

runCompletePipeline([
    repoUrl: 'https://github.com/your-repo/your-project.git',
    branch: 'main',
    buildCommand: 'mvn clean install',
    dockerfile: 'Dockerfile',
    imageName: 'sample',
    imageTag: 'latest',
    dockerRepoUrl: 'your-docker-repo',
    dockerCredentialsId: 'dockerhub-credentials'
])
